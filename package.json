{
  "name": "mkashima-portfolio-2",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "deploy": "next build && next export && echo mkashima.com > out/CNAME && echo > out/.nojekyll && git add out/ && git commit -m \"Deploy gh-pages\" && git subtree push --prefix out origin gh-pages",
    "test.export": "next build && next export && echo mkashima.com> out/CNAME && echo > out/.nojekyll"
  },
  "dependencies": {
    "next": "12.0.7",
    "react": "17.0.2",
    "react-dom": "17.0.2",
    "styled-components": "^5.3.3"
  },
  "devDependencies": {
    "babel-plugin-transform-define": "^2.0.1",
    "eslint": "8.4.1",
    "eslint-config-next": "12.0.7"
  }
}
